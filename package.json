{
  "name": "@jmlq/auth",
  "description": "JWT authentication package with clean architecture",
  "version": "0.0.1-beta.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/MLahuasi/jmlq-auth.git"
  },
  "homepage": "https://github.com/MLahuasi/jmlq-auth#readme",
  "scripts": {
    "dev": "rimraf dist && mkdir dist && tsc -p tsconfig.json",
    "build": "rimraf dist && mkdir dist && tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "jwt",
    "authentication",
    "clean-architecture",
    "typescript"
  ],
  "author": "MLahuasi",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@swc/core": "^1.3.95",
    "@swc/jest": "^0.2.29",
    "@types/bcryptjs": "^2.4.5",
    "@types/jest": "^29.5.8",
    "@types/node": "^20.8.10",
    "jest": "^29.7.0",
    "tsx": "^4.1.4",
    "typescript": "^5.2.2",
    "rimraf": "^6.1.2"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
